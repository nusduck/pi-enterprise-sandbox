# 浏览器验收测试记录（localhost:3000）

## 范围与环境

- 日期：2026-07-29（Asia/Singapore）
- 方式：浏览器真实用户操作（本机 `http://localhost:3000`）
- 账号：新建普通验收账号 `qa_ui_20260729`；密码未在本文档记录。
- 说明：按给定的 `admin / 123456` 首次登录被拒绝。仓库运行手册要求先通过管理员开通流程 provision 第一个管理员，未声明存在该默认账号。

## 测试结果

| 场景 | 操作与验收点 | 结果 | 证据/备注 |
| --- | --- | --- | --- |
| 登录失败反馈 | 使用给定管理员凭据登录 | 通过 | 页面明确显示 `Invalid credentials`，没有误登录。 |
| 注册与登录 | 注册普通验收账号并自动登录 | 通过 | 顶栏显示 `qa_ui_20260729`，状态显示 `Registered as qa_ui_20260729`。 |
| 新会话与基础对话 | 发送“只回复 OK、不调用工具” | 通过 | 创建会话并完成运行，状态 `Succeeded`，耗时约 5 秒，助手回复 `OK.`。 |
| 流式运行状态 | 运行期间检查活动面板、停止/跟进控件和 Inspector | 通过 | 显示 `Running`、运行 ID、会话 ID 和活动事件；完成后状态正确转为 `Succeeded`。 |
| 工具调用 | 请求仅创建 `qa_check.txt`，内容 `QA_OK` | 通过 | `write` 工具完成，耗时约 87 ms；未发生网络操作。 |
| 交付物 | 将 `qa_check.txt` 提交为交付物 | 通过 | `submit_artifact` 完成，生成工件 `QA Check`（5 B）。 |
| 交付物下载 | 点击交付物下载入口 | 通过 | 浏览器下载事件完成。 |
| 附件上传 | 上传 29 B 的验收文本文件 | 通过 | 草稿区显示 `qa-ui-upload-20260729.txt` 与 sandbox 数据集路径，发送按钮恢复可用。 |
| 运行记录 | 打开 Runs 并筛选 Completed | 通过 | 两条验收运行可见；工具运行显示 `Tool 2 · submit_artifact`、耗时 00:10。 |
| 审批中心 | 检查待审批列表和状态筛选 | 通过（空态） | 正确显示无待审批；本次安全工具未触发审批，未覆盖批准/拒绝决策。 |
| 能力注册表 | 已认证用户查看 Skills | 通过 | 返回共享 Skill 列表，并显示启用状态和来源。 |
| 计划任务表单 | 空值提交、Cron/一次性任务切换、已认证列表加载 | 通过（表单） | 空值提示 `A name and prompt are required.`；一次性任务正确显示 `Run at`；列表为空。未创建持久化计划，以避免在未经单独确认时留下后续自动运行。 |
| 管理员专属 A2A | 打开管理员协作设置 | 未覆盖 | 当前账号是普通用户，且未提供可用管理员账号；管理员凭据的首登验证失败。 |

## 需关注项

1. 初次提交交付物后，活动面板曾显示 `No download URL`；附件上传建立/刷新 sandbox 会话后，交付物出现有效下载链接且下载成功。最终用户路径可用，但“未先建立 workspace 会话时的交付物下载链接”是否应立即可用，建议产品/后端确认。
2. 给定的管理员账号不可用不是登录页面缺陷的充分证据：代码和运行手册均表明管理员需要显式开通。但若部署说明承诺了 `admin / 123456`，则部署初始化缺少该账号或凭据不一致。

## 未覆盖的高风险/持久化操作

- 审批的批准、拒绝、过期和取消。
- 创建、启停、立即运行、编辑和删除持久化计划任务。
- 管理员权限下的 A2A 凭据与审计操作。

