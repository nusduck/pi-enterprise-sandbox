# Refactor closure evidence (archived 2026-08-21)

The 25 rows below were the "Closed in the current refactor" section of
[`review-deferred-items.md`](../../review-deferred-items.md). They record how
each item was closed during the refactor that ended 2026-07-31.

They are kept because a few of these closures are finer-grained than any
`STATUS.md` row and this is their only written record. They are **history**:
do not cite them as current state without re-verifying against code.

## Closed in the current refactor

| Item | Closure evidence |
| --- | --- |
| PR-05 worker-default documentation lag | `createWorkerServices` → `ensureWorkerRunExecutorFactory`; the stub remains non-production/test-only. |
| plan §8.15 artifact unique key lag | The DDL now documents the migration's full-path `relative_path_hash` generated column and `UNIQUE(run_id, relative_path_hash, sha256)`, including the mandatory raw-path comparison. |
| `SANDBOX_MAX_OPEN_FILES` operator catalog | `.env.example`, base Compose, and the README execution-limit table now expose the implemented default `256`; production validation still enforces `16..65536`. |
| Agent/worker runtime env parity | Base Compose now gives both services the same model, prompt, MCP/profile, approval, skill, stream, and Sandbox policy knobs. The production overlay explicitly pins worker policy/isolation to the same strict values as the HTTP agent; HTTP-only A2A/listen/migration keys remain intentionally separate. |
| Root Python Redis dependency | `pyproject.toml` now declares `redis>=5.0`; `uv.lock` resolves the package, so host `uv` installs include the `redis.asyncio` client used by internal-plane wiring. |
| Production Sandbox transport wiring | `ServiceContainer.createPiRunExecutorFactory` now creates a per-Run transport and injects the formal HMAC clients for session provisioning, `files.read`, `files.write/edit`, `bash/python`, all four `process_*` methods, and `submit_artifact`; adapter tests cover Python args and process cursor mappings. |
| Inflight reconciliation for formal tool runtimes | Internal-plane shutdown now retains and reconciles `FilesReadRuntime`, `FormalExecutionRuntime`, `FilesWriteRuntime`, `FormalProcessRuntime`, and `FormalArtifactRuntime` before MySQL teardown; runtime and lifecycle tests cover UNKNOWN recovery and retained inflight claims. |
| A2A credential administration API/UI | The Agent exposes internal admin-only issue/rotate/revoke/config routes, the BFF owner-preserving proxy exposes `/api/a2a/*`, and `/settings/a2a` provides credential, endpoint, recent-task, and audit management. `agent/tests/a2a/a2a-admin.unit.test.js` covers tenant/admin boundaries and lifecycle actions. |
| A2A artifact byte streamer | Agent HTTP startup injects `createA2aArtifactByteStreamer`, which resolves the owner-scoped Run and Agent Session, then streams from Sandbox by opaque `artifact_id` only. Unit tests cover fail-closed missing-session/path-only cases; the live gate in `docs/evidence/release-gate-2026-07-19.md` verifies public 200 bytes/hash equality, cross-client 403, and `a2a.artifact_download` audit correlation. |
| Public Process status contract | Formal Sandbox responses and both Agent process transports normalize to the lowercase public `ProcessStatus` vocabulary. Sandbox-only transient states map at the public boundary, unknown states fail closed, and formal/tool-result tests cover the contract. |
| Worker runtime implicit stub | `createRunWorkerRuntime` now requires an injected executor/factory or an explicit `allowStubExecutor: true`; missing production wiring fails with `RUN_EXECUTOR_NOT_CONFIGURED`, while `ServiceContainer` continues to own the environment-gated non-production stub decision. |
| HealthResponse internal-plane status | `/health` reports enabled planes as `not_checked` (preserving liveness semantics), while `/ready` exposes `internal_plane_status=disabled|ready|not_ready` without connection details or secrets. Integration tests cover disabled and failed enabled-plane states. |
| Production Compose merge proof in CI | The Compose CI job now renders `docker-compose.yml + docker-compose.prod.yml` with non-secret production placeholders and runs `scripts/verify_compose_prod_config.py`. The verifier asserts nginx-only 80/443 publishing, one-shot migration isolation, migration dependency gates, and disabled runtime migration. |
| Compatibility Skill mount paths | Agent defaults, Sandbox policy, Bubblewrap, Dockerfiles, and Compose expose Skills only at the two canonical roots `/home/sandbox/skill` (bundled system tier) and `/home/sandbox/skill-user` (installed user tier); both are read-only to Sandbox, and production validation rejects any other path. |
| Skill lifecycle tools wired into the Agent runtime | The first-party `skill-lifecycle` extension exposes listing plus approval-gated upload install, Agent generation, edit, and uninstall operations. Archive bytes are resolved from the current turn's owner-scoped Dataset attachment; every mutation is confined to the user tier, committed atomically, and automatically refreshes the package registry. |
| Per-tool risk levels drive approval | `config/agent/tool-risk.json` (or `TOOL_RISK_POLICY_JSON`) is the platform risk table; AgentVersion `configJson.toolPolicy` and `mcpServers[].toolPolicy.riskLevel` / `.toolRiskLevels` form a lower layer that may only tighten it. The policy engine derives every allow/require_approval/deny from the resolved risk level, and the audit record names the risk source. |
| AgentVersion compaction policy wired | `applyContextPolicy` was dead code; `bindAgentVersionConfig` now surfaces `configJson.contextPolicy` and `PiRuntimeFactory` applies it to the per-run Pi settings manager. `session.compacted` is deduped on the compaction entry id instead of a field the SDK event does not carry. |
| Persistent Sandbox `/tmp` decision | [ADR 0004](adr/0004-session-persistent-tmp.md) accepts the existing Agent Session-private persistent temp tree. Bubblewrap binds only the current Session directory, `0700`/path checks/quota apply, Workspace and temp cleanup are coupled, and control-plane staging/Artifact roots remain outside the mount. |
| Sandbox service-token request-time resolution | Existing Sandbox client instances now call `resolveSandboxAuthHeader()` for every request instead of reusing the module-load `AUTH_HEADER` snapshot. Rotation tests prove a live client observes a changed token, and service identity headers override untrusted extras. |
| SSE history/cutover MySQL retry | Initial history, terminal confirmation, and history-to-live cutover reads now retry transient MySQL failures up to three total attempts with abort-safe waits. Ownership/input errors fail immediately, persistent failures preserve the original error, and sequence dedupe remains intact. |
| A2A credential-bound Agent version propagation | `SendMessage` now passes the credential-bound `agent_id` into `CreateRunService`; `RunParentProvisioner` resolves and validates that Agent's active immutable version inside the owner transaction. New sessions bind that version, reused sessions remain pinned to their original version, and cross-Agent conversation reuse fails closed. |
| Sandbox SQLite compatibility removal | `sandbox/database.py`, `repositories.py`, legacy managers, cleanup, and public authority routers are deleted. `tests/test_sandbox_mysql_import.py` checks filesystem absence, import absence, and absence of the legacy marker in all Sandbox Python sources; the offline suite uses only fakes or an unreachable MySQL-shaped DSN. |
| Live Sandbox isolation and Dataset/artifact gate | `scripts/release-gates/sandbox-live-gate.mjs` passed in a fresh non-privileged Bubblewrap container against a dedicated MySQL schema: cross-tenant/session checks returned 404, 20 executions overlapped, and a 5 GiB streamed Dataset completed with bounded RSS and matching Artifact bytes/hash. |
| Real 100-client SSE and 50-Run load gate | `scripts/smoke-cross-service.mjs` passed with separate Agent/Replay Redis containers (database index 0): 50 Runs succeeded and 100 real SSE clients received 1,100 monotonic sequenced frames. |
| Browser-facing Dataset → Artifact flow | The cross-service artifact smoke passed through BFF upload, Agent/Pi Python, `submit_artifact`, refresh queries, and public download; the downloaded report contained Dataset-derived data. |
