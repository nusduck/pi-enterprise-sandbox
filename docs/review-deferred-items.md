# Review deferred items

Non-severe / non-blocking items only. **Do not** park unresolved severe
vulnerabilities or unfinished PR-07 security work here.

| Item | Evidence | Risk | Benefit | Decision needed |
| --- | --- | --- | --- | --- |
| Child quota residual inter-sample race | Bounded monitor only; hard isolation is operator-asserted volume/project quota (`HARD_BACKEND_ASSERTED`) | Residual until live gate sets hard backend | True hard multi-tenant disk | Provision XFS/project/volume quota then set HARD_BACKEND_ASSERTED |
| Agent-worker skill/model env parity with HTTP agent | Compose now shares model/cwd; full MCP/prompt env matrix not duplicated | Low if LLM/cwd set | Identical knobs | Optional compose anchor/YAML anchor for shared agent env |
| docs/pr05-session-recovery.md still says Pi factory not worker default | Code now wires factory in createWorkerServices | Docs lag only | Accurate runbook | Docs-only follow-up |
| plan.md §8.15 still shows UNIQUE(run_id, relative_path, sha256) | Implementation uses relative_path_hash generated column for InnoDB 3072 | Docs lag only | Plan text accuracy | plan.md update when allowed |
| Agent HTTP service still uses process-shared sandbox client patterns on some routes | Worker hot path is per-run scoped; BFF/HTTP agent may differ | Low if BFF uses JWT | Uniform request-scoped clients | Follow-up only if HTTP agent concurrent sandbox tools race |
| config.AUTH_HEADER captured at module load | Worker process starts with compose env already set | Low for containers | Hot-reload token without restart | Ops: recreate agent-worker on token rotate |
| Bubblewrap `--unshare-cgroup` | plan §16.4 lists it; current prepare/preflight omit it | Low until cgroup escape is shown in this container profile; unverified flag can break launch | Stronger cgroup isolation when runtime guarantees support | Confirm host/container `bwrap` + cgroup ns policy, then enable with preflight |
| Compatibility skill mounts (`/sandbox/skills`, `/app/.pi/skills`) | `BubblewrapIsolationBackend.prepare` still ro-binds skills to three destinations | Low (read-only) | Drop once agents use only `/home/sandbox/skill` | Compatibility cleanup PR |
| Persistent `/tmp` vs plan `--tmpfs /tmp` | Current design binds persistent per-workspace temp to `/tmp` | Medium product impact if switched without tool migration | Simpler ephemeral temp | Product decision |
| Historical naming (`workspace_path` DB column) | Dual field after lease/symlink removal | None for security | Cleaner schema naming | Hygiene PR only |
| Dev `auth_enabled=false` offline create | Local suites create sessions without HMAC proof | None if production always has auth on | Hermetic tests without Agent provisioner | Never ship compose/prod with auth off |
| Claim capability probe scope | Probe checks PR-07B claim columns + two uniques only (not every Agent table) | Low if migrations applied in order | Faster startup | Expand probe if claim path gains more required indexes |
| HealthResponse plane field | `/ready` ANDs plane readiness when enabled but does not surface a dedicated plane status field | Low | Ops visibility | Optional response field once plane is production-enabled |
| redis package in pyproject service deps | Lazy import of `redis.asyncio` in wiring; listed in container `sandbox/requirements.txt` but not root `pyproject.toml` | Medium if host `uvicorn` without container image | Clean host install path | Add `redis` to service deps in a lock-aware PR (this task forbids lock edits) |
| Redis ACL for replay keyspace only | Compose uses shared password; docs recommend minimal authority | Low until multi-tenant Redis sharing | Least privilege | Ops ACL: `sandbox:internal:replay:v1:*` only |
| Live Redis/MySQL readiness soak | Offline suite injects fakes; no live ping gate in CI here | None for offline | Production confidence | Staging connectivity checklist before cutover |
| Redis ACL on shared process | Compose uses **dedicated** sandbox-replay-redis; ACL user on shared Redis not wired | Low with dedicated service | Multi-tenant hardening | Optional ACL file if consolidating instances |
| Live ACL command denial | Offline asserts policy document only | None offline | Confirm SET-only user rejects GET/SELECT | Final gate on real Redis |
| Inflight reconcile coverage beyond files.read | Only FilesReadRuntime tracks claims today | Medium when more internal tools ship | Full plane recovery | Register inflight in other tool runtimes |
| Per-child controlled egress proxy | Production rejects `allowlist`; no netns+proxy path yet | None while production is `disabled` only | Safe package installs / selective egress without container-wide firewall | Design + implement proxy; then re-enable production allowlist |
| Dev allowlist/unrestricted peer reachability | Dev modes share container net on `backend_internal`; children can reach peer service IPs | Dev-only | Convenience for local tooling | Keep prod disabled; optional stricter dev unshare |
| Remove legacy iptables env from local `.env` | Operators may still have `SANDBOX_IPTABLES_*` / `SANDBOX_ALLOWED_CIDRS` in private `.env` (ignored) | None | Cleaner operator env | Manual cleanup; not committed |
| Archive / field docs still mention iptables | `docs/archive/*`, some field-issue rows | None (archive) | Historical accuracy | Optional archive note; no code path |
| README/deployment long-form network runbook | Core tables updated; deeper diagrams may lag | Low | Operator education | Follow-up docs PR |
| Live `docker compose … config` merge proof in CI | Offline tests assert `!reset []` text + model; engine merge not executed here | Low if CI already runs `compose config -q` | Full rendered port map assertion | Wire gate: `docker compose -f docker-compose.yml -f docker-compose.prod.yml config` and assert only nginx publishes |
| Live MySQL dual-write for process_executions | ProcessManager dual-writes via injected formal port; offline uses FakeFormalProcessRepository | Medium until staging MySQL plane proves create/update under owner scope | Full multi-tenant process ledger in shared MySQL | Staging gate with real MySQL + sandbox plane enabled |
| Wire production sandboxTransport for process_read/python args | Agent tools + sandbox-client helpers exist; bundle still injects transport at run factory | Medium until BFF/worker injects full 10-method transport | End-to-end tool → HTTP mapping | Wire factory adapter mapping camelCase cursors to Sandbox REST |
| Process stream logs on durable disk (not only memory + snapshot columns) | Cursor reads use StreamLogBuffer + DB snapshot/chunk fallback; no separate rotating files yet | Low while log caps are enforced | Cheaper huge-output retention | Optional stdout_path/stderr_path file sinks under session temp |
| Cross-container process reclaim | Restart recovery marks LOST and identity-kills only when pid+start_identity still match on **this** host | None if Sandbox is single-instance; multi-host needs no reclaim of foreign PIDs | Honest LOST without false control | Keep fail-closed; do not invent attach across hosts |
| macOS start_identity strength | Uses `ps -o lstart` when `/proc` absent; weaker than Linux starttime jiffies | Dev-only PID reuse race | Stronger identity on Linux prod | Acceptable; prod Linux uses `/proc` |
| Unify process status casing with contracts | Runtime uses lowercase `running`/`lost`; packages/contracts ProcessStatus uses `running`/`completed` | Low (mapping at BFF) | Single public enum | Align camelCase agent tool results in transport adapter |
| process_start HTTP still shell-wrapped | Managed process argv is `bash -c command` under isolation (same as bash tool) | Medium for untrusted command strings | Eventual argv-array process_start | Policy already hard-denies; optional later argv mode |
| Dataset multipart chunked/resume API | plan §17.1 optional multi-part (`dataset-uploads` + parts); PR-09 ships single-stream only | Low for phase-1 | Resumable multi-GB uploads | Product decision; stream path already no full-buffer |
| Optional virus scan on Dataset ready | plan §17.4 optional | Low until scanner available | Malware gate before agent read | Ops integration |
| BFF conversation ownership gate before dataset proxy | Proxy requires session_id + conversation_id; full conversation owner ACL is PR-10 | Low while sandbox session ownership holds | End-to-end conversation ACL | PR-10 only |
| Legacy SQLite `artifacts` table lacks sha256/run_id columns | Formal plane + in-memory hold sha256/identity; SQLite session rows remain compatibility | Low for formal path | Single store | Optional drop when MySQL-only |
| Content-Disposition non-ASCII filename* clients | Server emits RFC 5987 `filename*` + CRLF strip | Low | Better CJK download names | Browser matrix only |
| Multi-host quota without shared control volume | Control-plane `control_root/quota` + fcntl covers multi-worker same host | Low while single-instance/shared control volume | Cross-host quota | Topology decision |
| Live MySQL + control volume mount gate | Offline: FakeFormal + immutable artifacts_root snapshots + restart recovery; compose mounts `SANDBOX_ARTIFACTS_ROOT` / `SANDBOX_CONTROL_ROOT` (never bwrap-bound) | None for offline suite | Connectivity + mounts present | Staging: MySQL ping + verify artifacts/control volumes exist and are not workspace binds |
| Document `SANDBOX_MAX_OPEN_FILES` in `.env.example` / compose | New RLIMIT_NOFILE setting default 256; wired in Settings + production ranges; env catalog not updated (this PR forbade compose/lock/.env.example edits) | Low (code defaults apply) | Operator discoverability | Docs/env-catalog follow-up |
| cgroup memory/cpu (vs RLIMIT_AS/CPU only) | PR-07 hard limits use setrlimit in child preexec; no memory.high / cpu.max cgroup v2 | Medium for hostile multi-tenant share of host | Stronger isolation under nested abuse | Optional cgroup path after bubblewrap `--unshare-cgroup` decision |
| RLIMIT_NPROC accounting under shared container UID | NPROC counts all processes for the sandbox UID (not per-session); tighten-only apply avoids host-test fork death | Low with per-session managed caps | True per-session PID budget | User-ns + separate UID map or cgroup pids controller |
| Live Linux container gate for FSIZE/NOFILE SIGXFSZ | Offline suite asserts inheritance + best-effort over-limit; macOS may differ on signal delivery | None offline | Confirm SIGXFSZ/EFBIG under prod image | Staging smoke: write > max_file_size_mb and open > max_open_files |
| Cancel full idempotency_records response replay | Create path stores full response; cancel is first-writer durable intent + key required, not full stored response replay | Low (intent is durable) | Perfect cancel key replay across BFF retries | Optional: store cancel response JSON under operation `cancel_run` |
| BFF direct Redis stream (no Agent hop) | PR-10 keeps Agent as SSE authority; BFF byte-proxies | Low latency extra hop | One fewer hop for live tail | Topology decision; would require BFF Redis + owner ACL parity |
| SSE `event:` named types vs legacy nested `data.event.type` | PR-11 normalizes platform envelopes + BFF relay; dual-path still supports Agent legacy types | Low | Drop legacy Agent adapter once Agent only emits platform envelopes | Follow-up when Agent wire is platform-only |
| Live Redis XREAD BLOCK | Offline uses non-blocking `readAfter` + poll; no dedicated blocking connection | Low (pollMs ~400) | Lower live latency under sparse events | Optional: duplex Redis client + abort on disconnect |
| Agent HTTP factory injects eventSseService in all unit fakes | Factory falls back to MySQL poll path when SSE service omitted | None offline | Uniform production path in all harnesses | Test hygiene only |
| Virtualized timeline for 10k+ tool cards | Activity drawer maps full run tool/process lists; process logs capped at 256KiB/stream | Low for typical runs | Avoid DOM growth on extreme runs | Optional windowing if field shows large timelines |
| Trace panel span tree completeness | Spans projected from tool/model/artifact events only; no OpenTelemetry backend yet | Low | Full distributed trace UI | Wire when observability exports span trees |
| Dataset delete/replace controls | Panel is read-only status + path; upload still via composer/attachments | Low | Full §19.7 ops | Product UX when DELETE dataset API is productized |
| Approval decision reason input field | Approve/Reject buttons only; no free-text reason box in card | Low | Auditability of deny reasons | Optional form control |
| A2A ListTasks + push notification configs | PR-12 implements Send/Get/Cancel/Subscribe + Agent Card; ListTasks and push webhooks deferred | Low (client-scoped isolation still enforced on Get/Subscribe) | Full A2A surface | Product when multi-task dashboards need enumeration |
| A2A credential admin HTTP API | Issue/rotate/revoke are application services; no public REST admin routes yet | Low (ops can call services / future admin BFF) | Operator UX for key rotation | Admin panel PR |
| A2A artifact byte streamer on Agent | Fail-closed: no `file.uri`; download route 503 without `streamArtifactBytes`. Agent lacks BFF-style Sandbox artifact-id stream | Medium until A2A binary delivery needed | Real download | Inject owner-scoped Sandbox download by artifact_id only; re-enable URI mint only with streamer |
| CreateRun uses tenant default agent version for A2A | a2a_tasks.agent_id is credential-bound; Run.agentVersionId still from default catalog agent | Low for single-agent tenants | Multi-agent version fidelity | Pass bound agentVersion into CreateRun provisioner |
| A2A blocking SendMessage (return_immediately=false) | Always non-blocking create + optional stream | Low | Spec default blocking mode | Optional wait-for-terminal path |
| Live Redis XREAD BLOCK on A2A stream | Same as BFF SSE: poll + readAfter | Low | Lower latency | Share duplex Redis client work |
| GetTask artifact scan safety ceiling | Hard fail over 10k events / 500 artifacts (no silent truncate) | Low for typical runs | Streaming artifact index | Raise limits or dedicated index table |
| Full SQLite backend removal from sandbox | Offline conftest + many unit tests still inject `sqlite://` for non-Run subsystems; production rejects non-MySQL | Medium product impact if tests rewritten mid-flight | Single MySQL code path | Drop SQLite backend after offline suite is MySQL-fake-only |
| Drop unused Sandbox `AgentSessionRepository` / agent_* Pydantic models | PR-13 severe removed HTTP managers; repos/models may still sit in `repositories.py`/`models.py` for retention DDL only | None for security (no route) | Smaller code surface | Hygiene PR after retention no longer needs `AgentRunRepository` purge |
| Delete `enterprise-agent-kit` package | Still listed in `agent/package.json`; tests exercise kit tools | Low (production uses three extensions) | Less dual tool registration risk | Dep-removal PR (requires lockfile; forbidden in PR-13 constraints) |
| Dockerfile empty `/sandbox/workspace` dir | Not a symlink; leftover mount point from old presentation link | None | Cleaner image | Dockerfile hygiene PR |
| HTTP `POST .../artifacts/register` alias | Already calls `submit`; name is legacy | None | Single URL | Optional alias drop after clients all use `/submit` |
| SSE history/cutover MySQL errors abort stream (live loop retries) | `RunEventSseService.openStream` only catches MySQL throws inside the live poll loop; history drain + cutover drain rethrow | Low (client reconnects with `afterSequence` / Last-Event-ID; no Run cancel) | Smoother open under brief MySQL blips | Optional: wrap phase-1/2 drains in same retry as live |
| Offline concurrent CreateRun without real InnoDB locks | Fake knex serializes txn in PR-14 load tests; true multi-writer CAS races need live MySQL | None offline | Prove idempotency under real row locks | Live gate: concurrent CreateRun same key on MySQL |
| ru_maxrss constant-memory proof for multi-MiB stream | Platform units differ (Linux KB vs macOS bytes); peak RSS noisy under GC | None (chunk-size instrumentation is authoritative offline) | OS-level RSS soak | Live gate optional; offline uses max `os.read`/`os.write` ≤ chunk |
| Full 5 GiB sparse stream end-to-end Dataset upload | Offline asserts 5 GiB constant + early TOO_LARGE on sparse + chunk-bound copy | Low offline | Plan §25.8 soak | Staging: stream ≥5 GiB Dataset with bounded RSS and workspace quota |
| 50 concurrent real Sandbox executions / 20 concurrent process starts | Offline uses fakes + ProcessManager unit caps | Medium until load-shaped | Capacity plan validation | Staging load with real Sandbox + Agent worker |
| Live Redis failure while SSE connected (kill redis mid-stream) | Offline injects `readAfter` throw → MySQL poll fallback | Low offline | Ops confidence | Staging: stop Redis, assert SSE continues from MySQL |
| Live MySQL failure fail-closed on CreateRun (connection refused) | Offline fake txn fail + queue fail | Low offline | Production error codes | Staging kill MySQL mid-create; assert no partial commits |
| Agent HTTP listen smoke under sandbox EPERM | `listen-smoke` may EPERM in restricted environments | None for logic | Local bind proof | Final approval: run listen/smoke outside sandbox |
| Real 100 concurrent SSE clients against Agent HTTP | Offline 100 concurrent `openStream` with fakes | Low offline | Wire+Redis backpressure under real sockets | Staging load generator + Agent |
| Migration 00006 (+ other ALTER-only ups) lack this-run partial DDL tracker | Core/00003/00009 use `withPartialDdlCleanup`; 00006 does `alterTable` then raw `CREATE TRIGGER` without tracker | Low after binlog preflight; mid-up failure can leave additive column without migration row | Symmetric cleanup for ALTER + trigger ups | Optional tracker for non-createTable DDL + orphan sentinels |
| migrateLatest preflight runs before knex migration lock | Orphan + trigger gates then `migrate.latest` (lock inside knex) | Low (lock serializes DDL; rare concurrent dual migrator) | Single locked preflight+DDL critical section | Only if multi-owner migrate races appear in ops |
| LeaseBusy/NeedsReconciliation `delayMs` unused by BullMQ worker | Errors thrown; `createRunWorker` relies on recovery re-enqueue + jobId=runId removeCompleted/Failed | None (job not completed; recovery path works) | Explicit delayed retry without recovery lag | Optional map `delayMs` to BullMQ `Worker` delayed failure |
| `createRunWorkerRuntime` silent stub if factory+executor both omitted | Production uses `createWorkerServices` → Pi factory; stub path is tests/dev | None on compose worker path | Fail closed if factory missing outside tests | Guard: require factory unless `AGENT_ALLOW_STUB_EXECUTOR` |
| Shared MySQL coincidental core table names trip orphan gate | Gate assumes dedicated agent schema | Low if shared multi-app DB | Namespaced tables or schema | Ops: dedicated database per service |
