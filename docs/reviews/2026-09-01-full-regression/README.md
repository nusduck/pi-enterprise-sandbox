# 当前功能全量真实场景回归

本目录在 2026-09-19 按 `refactor/updrdb-dbpm` 更新待执行案例，核对基线为 `480bf65d5bae8bf674599755e7469991c2ac0c38` 及本轮开始时已有未提交改动。本次是**测试设计更新，未执行新一轮产品回归**。

当前共 **95 个案例**：保留已有 76 个编号并校正断言，新增 19 个；旧索引写“73 个”，本轮逐标题核对为 76（含 A2A 三条），已修正。覆盖 9 条业务/运维旅程，每个案例的多种状态/格式/错误分支分别取证。**模型统一为 `deepseek-flash`**，包括子任务、定时任务、A2A 和恢复后的请求。

- [测试案例](test-cases.md)：§1–§3 为本轮执行规范、业务旅程和逐项案例；§4–§9 原样保留 2026-09-01 至 09-03 的历史记录。
- [真实资料与验算规则](data-and-oracles.md)：资料来源、冻结方式、四类办公件验收和证据模板。
- [全量覆盖矩阵](coverage.md)：当前页面、工具、HTTP 能力和 §32 验收面的对应关系。
- [TypeSafe 选例与取舍](case-selection.md)：硬性覆盖规则、逐项判断标准、日常场景选择和分支增量；本轮采用 skill 方法核对，没有调用 Jev 服务或生成概率评分。

“全量”指覆盖当前代码可识别的用户能力、管理能力、外部集成，以及权限、失败、恢复和容量边界。它不表示所有输入组合穷尽，也不表示本轮已全部通过。历史固定标记和模拟链路的结果不能继承为新场景的通过证据；管理员、多身份、A2A、删除、预算等项目也不能因上一轮跳过而从清单中消失。

本轮以普通用户完成实际工作为主：真实项目交接 → 公开数据分析 → Excel/Word/PDF/PPT 交付 → 跨会话复用 → Skill 固化 → 定时更新；另行覆盖管理员发布 Agent、外部系统接入及故障恢复。每条案例同时检查**任务结果是否正确**与**平台状态是否一致**。

数据主线使用本仓库冻结文档/变更记录、世界银行官方公开数据，以及明确标注为合成演练的订单/退款和会议资料。S 包给出可复核输入与独立答案，无需等待用户提供私有资料；不得包装成真实订单、客户或会议决定。也可追加用户授权的脱敏业务资料。

执行前记录实际 commit/dirty 清单、镜像或 VM release、服务健康、账号角色/组织关系、flash/Skill/MCP 能力和预算配置。入口默认 `http://127.0.0.1:3000/`，以部署为准。使用真实 flash 链路；检查 `AGENT_ENABLE_FAKE_LLM`/stub 未启用，最终上游请求的 `model` 为 `deepseek-flash`；模型不可用记阻塞，不切换。工具结果、SSE 和数据库记录不得手工伪造。环境依赖缺失记阻塞，实现与需求不符记失败/缺口；不能让模型换条路完成任务后把原能力标为通过。

操作仅使用本轮资源清单内的专用账号、会话、文件、进程和定时任务。案例包含测试资源的删除/撤销闭环，执行整轮时按已授权范围完成；共享栈重启、全局配置修改、真实业务资源删除须先明确操作范围。不得通过 `down -v` 清空现有环境复现空库；空库注册在独立环境执行。密码、Cookie、凭据及带签名下载 URL 不进入报告。

自建普通用户按注册功能完成，当前公开注册都落 `org_bootstrap`；跨组织与管理员跨组织分支必须另有合法预置身份。部署优先遵循当前 OrbStack K8s + Compose 依赖模式；DEPLOY 专项分开记录本地模拟与目标 UPDRDB/UPRedis/DBPM/VM 的证据。所有适用案例都需执行，不以 P0 或 TypeSafe 排序结果缩减全量范围。

执行结果另存带日期的新证据文件，逐条关联案例 ID 与子断言。六套自动化测试及类型检查按 [development.md](../../development.md) 执行，作为补充证据。只有真实证据使验收行状态变化时才同步 [STATUS.md](../../STATUS.md)；本次设计更新不改变该看板状态。

本次设计验证（2026-09-19，同上 HEAD + dirty 基线）：Python 3.11.15，`uv run --python 3.11 pytest -q tests/test_repository_layout.py tests/test_runtime_versions.py` → 28 passed；限定本目录的 `git diff --check` 通过。只读文本检查确认 95 个唯一 ID、95 个分组映射、覆盖矩阵无漏 ID、99 个本地链接目标存在、S1 金额独立重算为 540−75=465；历史 §4–§9 SHA-256 保持 `e954a2de5489bac0f3d447cd5eef6aeef55791a4b19729b25a4dfcc1eb66ebe6`。文本检查用宿主 Node v26.5.0，仅为文档核对，不作为项目 Node 22 运行验收。本轮未启动/重建容器、未跑六套业务测试或浏览器回归，原因是仅修改测试设计文档。
