/**
 * Run status is now rendered inline inside ConversationHeader (workbench toolbar).
 * Kept as a no-op export for any residual imports / tests that reference the path.
 */
export function RunStatusBar() {
  return null;
}
