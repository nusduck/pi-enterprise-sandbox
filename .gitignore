# Dependencies & build outputs
node_modules/
dist/
build/
.venv/
*.egg-info/

# Local secrets / env
.env

# Python / editor noise
.pytest_cache/
*.pyc
__pycache__/
*.db
.DS_Store
.coverage
htmlcov/
*.log
.idea/
.vscode/
*.swp
*~

# ── Runtime data (created by compose / local runs; never commit) ──────────
# Sandbox host bind mounts (docker-compose.yml):
workspaces/
tmp-workspaces/
artifacts/
control/
# Cross-service smoke temp root
.smoke-tmp/
# Live release-gate workspaces (scripts/release-gates/*)
.release-gate-*/
# Local / host-side Pi resource roots (container uses /app/pi-agent-home)
pi-agent-home/
agent/pi-agent-home/
agent/.a2a-live-agent-home/
agent/.a2a-live-agent-workspace/
